/.htaccess
/config.inc.php
/.buildpath
data/database.mwb.bak
/.idea/
/plugins.json
/xhprof
/phpmyadmin
/sitemaster
/tests/report
/.vagrant
/beanstalk_console
/wdn
/scripts/sitemaster_phantom_complied.js
/scripts/sitemaster_phantom_complied_test.js
# If plugins are ignored entirely and the metrics are supposed to go in there, then this rule won't work.
# Probably best for the plugins in external repos to go into this repo unless there is some reason they should/need
# to be developed externally.
# /plugins/
/node_modules/
